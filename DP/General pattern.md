1 normal dp is recursive  

better dp is tabular. 
in tabular when we need to use -1 index 

when u make tabular and find out how many previous rows you were using then that's optimal you start using prev arrays instead of a matrix.